services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: bank_cards
      POSTGRES_USER: bank_user
      POSTGRES_PASSWORD: bank_password
    ports:
      - "5434:5432"
    volumes:
      - bank_cards_data:/var/lib/postgresql/data

volumes:
  bank_cards_data: